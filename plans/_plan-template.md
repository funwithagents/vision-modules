# Plan title

**Status:** Todo

Implements the settled behavior in `specs/<spec>.md` ("<section>"). One-sentence statement of what this plan delivers and what it deliberately leaves out.

## Scope

The exact files this plan touches, each with a one-line note on what changes:

- `src/vision_modules/example.py` — what's added/changed here
- `tests/test_example.py` — functional tests for the above

## Steps

Ordered, buildable steps. Each should be concrete enough to implement without re-deciding the design (that's the spec's job) — this is the "how", the spec is the "what".

1. …
2. …

## Verification

How to confirm it's done: the specific tests to add/run, plus lint + type-check + full test run. Mark this plan `Done` (here and in [_index.md](_index.md)) only once all pass.
